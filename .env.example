# Copia questo file in .env e riempi i valori:
#   cp .env.example .env
#
# .env è in .gitignore: non committarlo mai, contiene le tue API key.
# Le variabili impostate nella shell hanno la precedenza su questo file.

# ── Provider LLM ────────────────────────────────────────────────────────
# Quale usare. Se lo lasci vuoto viene scelto in base alla key presente.
# Valori: gemini | anthropic
PROVIDER=

# Google AI Studio → https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Anthropic Console → https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Modello. Se lo ometti: gemini-3.1-flash-lite oppure claude-sonnet-5.
# Un modello dismesso costa un giro di rete a vuoto prima del ripiego, quindi
# conviene che questo valore sia vivo — il server lo dice nel log all'avvio.
MODEL=

# Catena di fallback, separata da virgola. Vuoto = default del provider.
MODEL_FALLBACK=

# ── Server ──────────────────────────────────────────────────────────────
# Se la cambi, aggiorna anche `SERVER` in extension/background.js: l'estensione
# non può leggere questo file, e senza quell'allineamento la chat dice solo che
# il server locale è irraggiungibile.
PORT=8787

# Identificativo del dominio richiesto da /v1/manifest
DOMAIN=dom_demo

# ── Limiti e costi ──────────────────────────────────────────────────────
# Ragionamento: THINKING_LEVEL sui modelli Gemini 3.x, THINKING_BUDGET sui 2.5.
THINKING_LEVEL=minimal
THINKING_BUDGET=

# Tetto ai token generati per risposta
MAX_OUTPUT=600

# Tetto di richieste al minuto verso il provider, su finestra scorrevole:
# finché si sta sotto non si aspetta nulla.
RPM=10

# Cache in memoria delle richieste identiche: on | off
CACHE=on

# Tetto di richieste al giorno; oltre, il server risponde 429
DAILY_CAP=600
